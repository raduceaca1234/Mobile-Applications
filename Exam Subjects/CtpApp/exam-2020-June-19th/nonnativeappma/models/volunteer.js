class Volunteer {
    constructor(id, identifier, name, status, passengers, driver, paint, capacity) {
      this.id = id;
      this.identifier = identifier;
      this.name = name;
      this.status = status;
      this.passengers = passengers;
      this.driver = driver;
      this.paint = paint;
      this.capacity = capacity;
    }
  }
  
  export default Volunteer;
  