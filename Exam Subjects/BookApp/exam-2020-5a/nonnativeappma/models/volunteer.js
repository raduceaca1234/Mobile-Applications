class Volunteer {
    constructor(id, title, status,student, pages, usedCount) {
      this.id = id;
      this.title = title;
      this.status = status;
      this.student = student;
      this.pages = pages;
      this.usedCount = usedCount;
    }
  }
  
  export default Volunteer;
  