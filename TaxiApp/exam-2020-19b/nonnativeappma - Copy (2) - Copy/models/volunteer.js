class Volunteer {
    constructor(id, name, status, size, driver, color, capacity) {
      this.id = id;
      this.name = name;
      this.status = status;
      this.size = size;
      this.driver = driver;
      this.color = color;
      this.capacity = capacity;
    }
  }
  
  export default Volunteer;
  