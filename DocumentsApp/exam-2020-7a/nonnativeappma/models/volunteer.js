class Volunteer {
  constructor(id, name, status, owner, size, usage) {
    this.id = id;
    this.name = name;
    this.status = status;
    this.owner = owner;
    this.size = size;
    this.usage = usage;
  }
}

export default Volunteer;
